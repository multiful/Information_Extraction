# 로컬 과제/메모 txt 파일 무시
# ('#'로 시작하는 파일명은 첫 글자를 escape 해야 함)
\## Information Extraction.txt
공통과제.txt

# HuggingFace 다운로드 캐시
docred_data/.cache/

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# OS
.DS_Store

# 모델 산출물 (재생성 가능) — 예측 결과/체크포인트. comparison.md는 추적함
results/*.json
results/*.pt
colab_bundle.zip

# Trained model checkpoints/logs/predictions (large or regenerable, via Scripts/models/*_train.py)
Scripts/models/**/checkpoints/
Scripts/models/*_checkpoints/
Scripts/models/logs/
Scripts/models/predictions/
*.pt
Scripts/models/*.log
Scripts/models/dk_filtered_distant.json

# DocRED 공식 스코어러의 train fact 캐시 (Scripts/docred_scorer.py가 재생성)
docred_data/data/_fact_cache/

# Re-DocRED revised 라벨은 main에 Git LFS로 관리 → dh엔 커밋하지 않음(로컬/LFS pull로만 사용)
docred_data/data/*_revised.json
